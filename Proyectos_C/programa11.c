#include <stdio.h>
#include <stdlib.h>

int main(){
       int i=0;
       do{
        printf("valor de 1 = %d \n", i);
        i++;
       }
       while(i<3);
       system("Pause");
       return 0;
}